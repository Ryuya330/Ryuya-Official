// Copy this file to config.local.js and put your real API key there.
// IMPORTANT: Do NOT commit config.local.js to a public repo.

window.SITE_CONFIG = {
    // Replace with your YouTube Data API v3 key if you want the API-driven feeds.
    // If you omit this or set it to 'YOUR_YOUTUBE_API_KEY', the site will fall back
    // to embedding the channel's uploads via iframe (no API key required).
    youtubeApiKey: 'YOUR_YOUTUBE_API_KEY'
};
